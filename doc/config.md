# 数据集

## 持续指令测试数据集（60条）

### G类：Goal Reasoning（20条）

#### G1（正常条件触发）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| G1-01 | 如果冰箱里有牛奶，就帮我用微波炉热一杯。 | **场景修改**：冰箱里有牛奶，微波炉正常使用。**预期行为**：智能体检测到冰箱里有牛奶，加热牛奶。**验证点**：条件触发、容器放入动作正确。 |
| G1-02 | 如果桌上有苹果，帮我洗干净拿过来。 | **场景修改**：桌上有苹果，属性是 dirty，水龙头（faucet）具备 HAS\_SWITCH，初始状态为 on（水龙头可正常出水）。**预期行为**：智能体检测到苹果是脏的，走到桌边拿起苹果，走到水龙头处打开水龙头冲洗苹果，使其变为 clean，然后拿过来给用户。**验证点**：条件触发、清洗动作正确。 |
| G1-03 | 如果电视机开着，就把遥控器拿过来。 | **场景修改**：电视机（television）具备`HAS_SWITCH`，初始状态`on=True`（开启）；遥控器（remotecontrol）放在沙发（sofa）上，具备`GRABBABLE`。**预期行为**：持续监测电视状态，检测到开启后走到沙发处抓取遥控器，然后拿回到电视所在位置（或机器人自身）。**验证点**：条件触发、目标定位准确。 |
| G1-04 | 如果灯亮着，就关掉它。 | **场景修改**：灯（light）具备`HAS_SWITCH`，初始状态`on=True`（亮着）。**预期行为**：持续监测灯状态，检测到亮着时走到灯处，按下开关将其关闭。**验证点**：条件触发、开关动作正确。 |
| G1-05 | 如果杯子里有水，就倒掉放进柜子里。 | **场景修改**：杯子里有水（FILLED\_WATER），柜子（cabinet）门关着。**预期行为**：智能体检测到杯中有水，拿起杯子走到水槽（sink）倒掉，再打开柜门放入空杯。**验证点**：液体倾倒与收纳动作正确。 |

#### G2（意图模糊/需问答补充）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| G2-06 | 如果窗帘拉上了，就把灯打开。 | **场景修改**：场景中存在落地灯（floorlamp）和泛指的灯（light）。**预期行为**：智能体检测到多个灯，无法确定具体指哪一个，应主动向用户提问：“您指的是电灯还是落地灯呢？”等待用户澄清后，打开指定物品。**验证点**：指代消解、主动询问、不随机执行。 |
| G2-07 | 如果看到杯子了，把它收到柜子里。 | **场景修改**：机器人看到一个杯子（cup），场景中有两个柜子。**预期行为**：智能体无法判断放到哪个柜子，主动询问。**验证点**：位置指代模糊时主动澄清。 |
| G2-08 | 把桌上温度合适的牛奶拿过来。 | **场景修改**：桌上分别有冷的牛奶和热的牛奶，智能体无法判断拿哪一个。**预期行为**：智能体主动询问：“您说的‘合适’具体指什么？是冷还是热？”**验证点**：模糊形容词需澄清。 |
| G2-09 | 如果书掉到地上了，就把它捡起来放到桌子上。 | **场景修改**：地上有多本书。**预期行为**：智能体主动询问：“您指的是哪一本书？需要我全部捡起吗？”**验证点**：目标物品指代模糊时澄清。 |
| G2-10 | 如果门开着，记得关掉。 | **场景修改**：场景中有冰箱门、柜子门和门（door）开着。**预期行为**：智能体应主动询问：“您指的是哪一个门？”**验证点**：物品指代模糊时澄清。 |

#### G3（条件恒不满足）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| G3-11 | 如果冰箱里有牛奶，就帮我用微波炉热一杯。 | **场景修改**：冰箱（fridge）被添加 `BROKEN` 状态，模拟冰箱损毁或无容纳空间。**预期行为**：智能体持续监测冰箱容纳物状态。经过一段时间（如30秒）应报告“冰箱里没有牛奶”，保持待命。**验证点**：识别不可能条件、持续监测、合理超时提示。 |
| G3-12 | 如果电视开着，就把它关了。 | **场景修改**：电视机（television）被添加 `BROKEN` 状态（开关失效），永远无法处于“开着”状态（初始`off`）。**预期行为**：条件永假，报告“电视机无法开启，条件不可能满足”。**验证点**：物理损坏导致条件永不成立。 |
| G3-13 | 如果电视机开着，就把遥控器拿过来。 | **场景修改**：电视机（television）被添加 `BROKEN` 状态（开关失效），永远无法处于“开着”状态（初始`off`）。**预期行为**：条件永假，报告“电视机无法开启，条件不可能满足”。**验证点**：物理损坏导致条件永不成立。 |
| G3-14 | 如果灯亮着，就关掉它。 | **场景修改**：灯（light）被添加 `BROKEN` 状态（开关损坏），永远无法亮起（初始`off`）。**预期行为**：条件永假，报告“灯无法亮起，条件不可能满足”。**验证点**：物理损坏导致条件永不成立。 |
| G3-15 | 如果炉子开着，就帮我热一杯牛奶和一杯咖啡拿到桌上。 | **场景修改**：炉子（stove）被添加 `BROKEN` 状态（开关完全损坏/缺失），因此炉子永远无法处于“开着”状态，初始状态为 off。牛奶（milk）和咖啡（coffee）放在桌上，杯子（cup）可用。**预期行为**：智能体持续监测炉子状态，因损坏，炉子永远无法被打开（on 状态永假），条件“如果炉子开着”永远无法满足。智能体经一段时间（如 30 秒）后报告“炉子开关损坏，无法开启，条件永远无法满足”，保持待命。**验证点**：物理损坏导致条件永不成立。 |

#### G4（路径不可达/手段缺失）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| G4-16 | 如果冰箱里有牛奶，就帮我用微波炉热一杯。 | **场景修改**：冰箱里有牛奶，但是微波炉被设置为 `hidden_broken`，模拟微波炉坏了的情况。**预期行为**：智能体检测到微波炉不可用，思考用户的真实意图是加热牛奶，选择炉子（stove）完成加热的动作。**验证点**：识别用户真实意图，路径不可达时思考替代措施。 |
| G4-17 | 如果桌上有苹果，帮我开水龙头冲一冲，洗一个拿过来。 | **场景修改**：桌上有苹果，但水龙头（ **faucet**）被设置为 `hidden_broken`（模拟水龙头打不开或者停水）。**预期行为**：旁边有水壶和碗，智能体判断水龙头只是提供水洗苹果的途径，改用wipe操作擦洗苹果使其变为clean。**验证点**：识别用户真实意图，路径不可达时思考替代措施。 |
| G4-18 | 如果电视开着，就用遥控器把电视关了。 | **场景修改**：遥控器被设置为 `hidden_broken`（模拟遥控器丢失或者没电）**预期行为**：智能体尝试抓取遥控器失败，报告“遥控器无法拿到”，并询问用户是否手动通过按动电视机按钮或者拔插头的操作关闭电视。**验证点**：识别物品损坏，尝试替代措施。 |
| G4-19 | 如果台灯亮着，就关了。 | **场景修改**：灯（light）亮着，但灯的开关按钮被设置为 `hidden_broken`（开关损坏），无法通过常规方式关闭。**预期行为**：智能体尝试按开关无效，报告“开关损坏，无法关灯”，通过拔掉插头的方式关灯。**验证点**：识别控制手段损坏。 |
| G4-20 | 如果桌子上有水壶，就给我倒杯水过来。 | **场景修改**：杯子被设置为 `hidden_broken`，无法拿取或使用。预期行为：智能体判断杯子只是容器，用户的目的是解决口渴的问题，改用碗装水。**验证点**：代替容器。 |

---

### M类：State-Consistent Memory（20条）

#### M1（物品短暂消失/瞬移）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| M1-01 | 如果电视开着，就把遥控器拿过来。 | **场景修改**：电视（television）初始`on=True`；杯子（cup）放在桌上，具备`GRABBABLE`。当智能体靠近遥控器时（距离<1米），动态事件触发：遥控器被临时移除`GRABBABLE`属性并设置为不可见（模拟被人拿走），3秒后恢复属性并重新出现在原位置。**预期行为**：智能体检测到电视开→走向桌子→尝试抓取遥控器失败（物品消失）→等待并重新搜索→3秒后物品重现→重新抓取成功→拿到电视旁。**验证点**：物品短暂消失时等待，重现后重新识别抓取。 |
| M1-02 | 如果桌子上有咖啡壶，就给我倒杯水过来。 | **场景修改**：桌子（table）上放有一个咖啡壶（coffeepot）。杯子（waterglass）放在咖啡壶旁边，具备`GRABBABLE`。当智能体伸手去拿杯子准备倒水时，动态事件触发：杯子被瞬移到厨房台面（kitchencounter）上（`teleport`）。**预期行为**：智能体持续监测桌面是否有咖啡壶。检测到咖啡壶后，走向桌子准备拿杯子倒水→伸手时发现杯子不在原位置（已瞬移）→智能体暂停当前动作，在周围环境搜索杯子→找到厨房台面上的杯子→拿起杯子，回到咖啡壶处，完成倒水动作。**验证点**：目标物品（杯子）在执行中途瞬移时，智能体能检测到状态变化，主动重新搜索并调整计划，最终完成任务。 |
| M1-03 | 如果你看到了脏杯子，就及时把它们放到水槽里。 | **场景修改**：脏杯子放在桌上。当智能体靠近杯子时，动态事件触发：杯子消失2秒后重现。**预期行为**：智能体抓取失败，等待后成功抓取并放入水槽。**验证点**：物品短暂消失。 |
| M1-04 | 如果沙发上有抱枕，就帮我拿一个过来。 | **场景修改**：沙发（sofa）上放有一个抱枕（pillow），具备`GRABBABLE`。当智能体靠近抱枕准备抓取时，动态事件触发：抱枕被临时移除`GRABBABLE`属性并设置为不可见（模拟被人拿走），2秒后恢复属性并重新出现在沙发上。**预期行为**：智能体持续监测沙发上是否有抱枕。检测到抱枕后，走向沙发准备抓取→伸手时发现抱枕消失→智能体暂停动作，在原地等待并继续监测→2秒后抱枕重现→智能体重新抓取抱枕→拿到用户身边。**验证点**：任务执行中目标物品临时消失时，智能体能等待并重试，最终完成任务。 |
| M1-05 | 如果牛奶是凉的，就把它放进微波炉加热。 | **场景修改**：牛奶（milk）放在桌上，智能体拿起牛奶时，动态事件触发：牛奶瞬移到冰箱内部。**预期行为**：智能体发现手中牛奶消失（或原位置消失），主动搜索周围环境，找到冰箱内的牛奶，再继续加热流程。**验证点**：物品瞬移后重新搜索。 |

#### M2（多个相似物体/实例混淆）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| M2-06 | 如果桌上有杯子，就把脏杯子放到水槽里。 | **场景修改**：桌上有两个外观相同的杯子：杯Adirty，杯Bclean。水槽（sink）具备`RECIPIENT`。**预期行为**：智能体检测到水龙头开→扫描桌上杯子→对比属性→选择dirty的脏杯→抓取→放入水槽。忽略干净杯。**验证点**：基于属性区分相同外观物品。 |
| M2-07 | 如果冰箱门里有苹果，就把红苹果拿出来。 | **场景修改**：冰箱（fridge）门开（`open=True`）。冰箱内有两个苹果：苹果A是红的，苹果B是绿的。**预期行为**：智能体检查苹果的颜色属性，选择红色的苹果拿出。**验证点**：基于颜色属性区分。 |
| M2-08 | 拿一个熟了的过来。 | **场景修改**：桌子上有两个苹果，一个是红的，一个是绿的。**预期行为**：智能体通过颜色属性，通过推理判断成熟度这个未知属性，选择红色。**验证点**：属性推理。 |
| M2-09 | 如果冰箱里有果汁，就倒一些在干净的杯子里。 | **场景修改**：冰箱内有两瓶液体：一瓶是牛奶（milk），一瓶是果汁（juice）。桌上有一个干净杯子（waterglass，状态 CLEAN）和一个脏杯子（状态 DIRTY）。**预期行为**：智能体需要区分液体类型（选择 juice），同时区分杯子状态（选择 CLEAN 的杯子），完成倒果汁动作。**验证点**：基于属性和状态的联合区分。 |
| M2-10 | 如果沙发上有黄色的抱枕，就帮我拿一个过来。 | **场景修改**：沙发上有两个抱枕（pillow），一个是黄色的，另一个是橙色的。**预期行为**：精准识别相近属性，选择黄色的抱枕。**验证点**：易混淆属性区分。 |

#### M3（动态全局规则变化）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| M3-11 | 如果冰箱里有苹果，就拿两个出来放到桌上。 | **场景修改**：设置全局规则"人在打扫冰箱时，不希望被打扰"，因此当规则生效时，智能体应该禁止靠近冰箱。**预期行为**：智能体持续监测全局规则，检测到规则动态解除后，智能体立即重新尝试取苹果，成功拿出。**验证点**：规则动态变化（阶段禁止→允许），智能体能感知规则变化并重新尝试执行，而不是直接报错或永久放弃。 |
| M3-12 | 如果电视关着，就把它打开。 | **场景修改**：全局规则：“主人睡觉时段禁止开电视”。初始时间未进入休息时段，智能体移动遥控器时时间进入休息时段，规则生效，阻止动作。后续规则解除。**预期行为**：智能体尝试被阻止后等待，规则解除后完成拿取。**验证点**：动态时段规则。 |
| M3-13 | 如果桌上有杯子，就把它放进柜子里。 | **场景修改**：全局规则：“柜子在消毒的时候不能把东西放进去”。初始未禁用，智能体拿着杯子靠近柜子时规则生效，禁止放入。之后规则解除。**预期行为**：智能体等待规则解除后再放入。**验证点**：动态禁令。 |
| M3-14 | 如果桌上有脏杯子，就把它们都收到水槽里。 | **场景修改**：桌上有三个脏杯子，全局规则：“厨房清洁期间禁止动餐具”。初始未禁止，智能体拿起第一个杯子时规则生效，禁止后续动作。之后规则解除。**预期行为**：智能体停止当前批次，等待规则解除后继续。**验证点**：任务中断后恢复。 |
| M3-15 | 如果牛奶是凉的，就把它放进微波炉加热。 | **场景修改**：牛奶在桌上，微波炉里有东西 模拟被其他人占用。全局规则：“微波炉一次只能被一个人使用”。智能体选择等待，直到我们把物品取出，微波炉可用，智能体继续执行操作。**预期行为**：智能体等待规则失效后再加热。**验证点**：规则不符时等待。 |

#### M4（记忆对齐）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| M4-16 | 如果你看到刚才用过的那个杯子，就把它放到水槽。 | **场景修改**：智能体在客厅操作一个杯子，使其状态变为脏，之后杯子出现在厨房。**预期行为**：智能体通过状态的对比进行记忆对齐，将厨房的杯子匹配到客厅使用过的杯子。**验证点**：状态记忆对齐。 |
| M4-17 | 如果柜子开着，就把你刚才洗好的杯子放进去。 | **场景修改**：桌子上初始有三个颜色不同的杯子，分别为红黄蓝，状态都为dirty，之后智能体清洁红色的，使其状态变为clean，红色杯子被移动到桌子上，但桌上本来就有一个dirty的红色杯子。**预期行为**：智能体搜索水槽，未找到，转而搜索桌面，找到记忆中红色的刚清洁过的杯子。**验证点**：记忆对齐，验证状态。 |
| M4-18 | 如果你看到刚才用的水壶，就再用它给我倒杯水。 | **场景修改**：初始时桌上有两个水壶，一个是红色，一个是绿色，智能体执行指令，用红色的水壶倒了一杯水，之后将两个水壶移动到柜子里。**预期行为**：智能体在柜子里找到水壶，并将两个水壶与记忆中的进行对比，再次选择红色倒水。**验证点**：属性对齐。 |
| M4-19 | 如果电视机没开，就把它打开。 | **场景修改**：智能体之前用灰色的遥控器操作电视，但是遥控器被从客厅移动到房间。**预期行为**：智能体通过对比各个属性，判断房间的遥控器就是电视遥控器，拿到客厅操作电视。**验证点**：记忆对齐。 |
| M4-20 | 如果桌上有苹果，就把洗好的那个放到冰箱。 | **场景修改**：初始柜子里有若干个红色和绿色的苹果，属性为dirty，智能体选择了某个红色的苹果清洗之后属性变为clean，所有的苹果被移到桌上。**预期行为**：智能体能通过多个属性的对比，判断找出刚才洗过的红苹果。**验证点**：属性记忆对齐。 |

---

### P类：SDG Planning（20条）

#### P1（资源竞争/依赖冲突）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| P1-01 | 如果桌上有水壶，就给我倒两杯水过来。 | **场景修改**：桌上有一个杯子和一个碗。**预期行为**：智能体思考判断杯子的用处是容器，当资源有限时，选择替代品碗来倒水。**验证点**：识别资源限制，寻找替代措施。 |
| P1-02 | 如果微波炉能用，就帮我热一下面包和牛奶。 | **场景修改**：限制微波炉一次只能放一个物品，牛奶和面包都需要加热。**预期行为**：智能体分两次加热。**验证点**：多物品分批处理。 |
| P1-03 | 如果冰箱里有鸡肉，帮我把加热好的鸡肉和肉排放到面包片上做三明治，做好放桌上。 | **场景修改**：鸡肉（chicken）和肉排（cutlets）都需要通过微波炉进行加热使其状态发生改变，但是微波炉一次只能加工一种食材（通过global_rules拦截）。**预期行为**：按顺序串行加工。**验证点**：单资源容量限制下的串行处理。 |
| P1-04 | 如果炉子开着，就帮我热一杯牛奶和一块肉排拿到桌上。 | **场景修改**：炉子保持开启状态，一次只能加热一种物品（通过global_rules拦截）。**预期行为**：智能体选择其中一样先进行加热，再操作另一件。**验证点**：单资源容量限制下的串行处理。 |
| P1-05 | 如果洗衣机没在工作，就把它打开，把地上这两堆脏衣服洗一下。 | **场景修改**：浴室地上有两堆脏衣服（clothespile）。洗衣机插电且可正常运行，但一次只能洗一堆（通过global_rules拦截）。**预期行为**：分两次洗衣服。**验证点**：串行规划。 |

#### P2（动作合并/并行优化）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| P2-06 | 如果你看到桌上有脏杯子，就把它们收到水槽里。 | **场景修改**：桌上有两个脏杯子（均无`POURABLE`），智能体双手各拿一个，一次性能运到水槽。**预期行为**：智能体同时抓取两个杯子，一次性走到水槽并放入。避免分两次单手往返。**验证点**：并行优化，利用双手资源。 |
| P2-07 | 如果冰箱里有苹果，就拿两个出来放到桌上。 | **场景修改**：冰箱内有两个苹果，双手各拿一个。**预期行为**：一次性拿出两个苹果。**验证点**：并行取出。 |
| P2-08 | 如果盒子里有苹果，就把它们都拿到桌上。 | **场景修改**：盒子里有两个苹果，双手各拿一个。**预期行为**：一次性拿到桌上。**验证点**：同上。 |
| P2-09 | 如果桌上有牛奶，全都倒进杯子里。 | **场景修改**：桌上有两瓶牛奶。**预期行为**：智能体双手各拿一瓶牛奶，同时倒入杯子中。**验证点**：并行动作。 |
| P2-10 | 如果桌上有遥控器，就把它们拿到沙发上。 | **场景修改**：桌上有两个遥控器（remotecontrol），双手各拿一个。**预期行为**：一次性拿到沙发上。**验证点**：并行搬运。 |

#### P3（严格复杂时序依赖）

| 编号 | 指令 | 详细描述 |
| --- | --- | --- |
| P3-11 | 如果牛奶是凉的，就把它放进微波炉加热。 | **场景修改**：牛奶（milk）放在桌上，初始状态为“凉”。微波炉（microwave）具备`CAN_OPEN`、`CONTAINERS`、`HAS_SWITCH`，但初始状态为：未插电（移除`HAS_PLUG`）且门关闭、开关关闭。墙上存在电源插座（electricaloutlet）。**预期行为**：智能体持续监测牛奶状态（认为凉触发）。为了加热牛奶，必须先让微波炉可用。智能体应：拿起牛奶→检查微波炉→发现未插电→寻找插座插电（恢复`HAS_PLUG`）→打开微波炉门→放入牛奶→关门→按下开关（`ON`）→等待加热完成→取出热牛奶。**验证点**：识别跨物品的状态依赖（牛奶的状态变化需要微波炉的插电状态），正确执行依赖链（插电→开启→加热）。 |
| P3-12 | 如果咖啡是凉的，倒进炉子里煮两分钟。 | **场景修改**：咖啡初始状态是凉的，炉子被占用。**预期行为**：智能体等待炉子空闲。**验证点**：容器被占用状态等待。 |
| P3-13 | 如果水壶里有水，帮我倒一杯，我要喝。 | **场景修改**：水壶里有水，桌面上放着杯子，但是杯子的属性是dirty。**预期行为**：智能体识别喝水的水需要是干净的，清洁水杯使其变为clean。**验证点**：依赖链：清洁杯子→倒水。 |
| P3-14 | 如果苹果是完整的，就用干净的刀把它切成块。 | **场景修改**：场景中有苹果（apple），新增切的动作，刀初始为dirty。**预期行为**：智能体检测到刀是脏的→清洗→刀变为clean→切苹果→苹果获得SLICED状态。**验证点**：依赖链：清洗→切。 |
| P3-15 | 如果桌上有一杯水，就把它放进冰箱冷藏。 | **场景修改**：冰箱具备`HAS_SWITCH`，但初始未插电（移除`HAS_PLUG`）。桌上有水。**预期行为**：智能体走近冰箱检查→发现未插电→插电→按下开关→冰箱开始制冷。**验证点**：依赖链：插电→开开关。 |
| P3-16 | 如果冰箱里有鸡，用微波炉热一只鸡。 | **场景修改**：微波炉（microwave）具备`HAS_SWITCH`和`HAS_PLUG`，但初始未插电。**预期行为**：智能体检查微波炉→插电→打开开关→微波炉开始加热。**验证点**：依赖链：插电→开开关。 |
| P3-17 | 如果冰箱里有派 (pie)，用干净的盘子装一些到微波炉里加热。 | **场景修改**：初始时盘子是脏的，应该先清洗干净。**预期行为**：清洗盘子→装物。**验证点**：同上。 |
| P3-18 | 如果衣服是脏的，就放进洗衣机清洗。 | **场景修改**：洗衣机（washingmachine）具备`HAS_SWITCH`和`HAS_PLUG`，初始未插电。**预期行为**：插电→按开始键→洗衣机启动。**验证点**：同上。 |
| P3-19 | 如果冰箱里有肉排，就把它放到面包片上。 | **场景修改**：冰箱（fridge）内有肉排（cutlets）。面包片（breadslice）存在。**预期行为**：智能体打开冰箱取出肉排→将肉排放到面包片上。**验证点**：依赖链：取物→放入。 |
| P3-20 | 如果冰箱里有肉排，帮我用锅煎一块肉排。 | **场景修改**：肉排需要通过锅进行烹饪，炉子初始没有插电/未开。**预期行为**：插电/开开关→开始烹饪。**验证点**：同上。 |